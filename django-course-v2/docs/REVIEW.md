# 🔍 بررسی اسکلت دوره جامع Django (نسخه فعلی)

> این گزارش بر اساس فایل‌های دریافتی نوشته شده: صفحه اصلی (`index.html`)، استایل (`style.css`)، اسکریپت تعاملات (`script.js`) و `README.md`. ساختار فصل‌ها از روی README (قالب ۱۴ بخشی) تحلیل شده است.

---

## ✅ نقاط قوت اسکلت موجود

| # | نقطه قوت | توضیح |
| --- | --- | --- |
| ۱ | معماری تمیز و آفلاین | پروژه HTML ایستا بدون هیچ وابستگی خارجی؛ قابل استفاده با دابل‌کلیک |
| ۲ | قالب آموزشی منظم | ساختار ۱۴ بخشی یکسان در همه فصل‌ها — الگوی بسیار خوب |
| ۳ | ذخیره پیشرفت | `localStorage` با کلید `djc_finished_v1` و نمایش ✔ روی کارت‌ها |
| ۴ | جست‌وجو و فیلتر | جست‌وجوی زنده با `data-search` + فیلتر ۵ بخشی |
| ۵ | تعاملات پایه | نوار پیشرفت مطالعه، کپی کد، فهرست شناور با scroll-spy، پیمایش با کیبورد |
| ۶ | طراحی واکنش‌گرا | چیدمان مناسب موبایل و حالت چاپ |

---

## ⚠️ کمبودها و فرصت‌های حرفه‌ای‌شدن

### الف) آموزشی (مهم‌ترین‌ها)

| # | کمبود | راه‌حل در قالب جدید |
| --- | --- | --- |
| ۱ | **واژه‌نامه تخصصی وجود ندارد** — اصطلاحات انگلیسی (ORM، CSRF، ViewSet...) بدون ترجمه و توضیح در متن رها شده‌اند؛ برای مخاطب «صفر مطلق» این بزرگ‌ترین مانع فهم است | سامانه واژه‌نامه سه‌لایه: ① تولتیپ هاور/لمس روی واژه‌های خط‌چین‌دار ② «واژه‌نامه پایان فصل» خودکار (بخش ۱۵) ③ صفحه «واژه‌نامه جامع دوره» با دسته‌بندی و جست‌وجو |
| ۲ | **آزمونک فصل استاتیک است** — فقط متن پرسش و پاسخ داخل `details`؛ بدون انتخاب گزینه، نمره، بازخورد و انگیزه | موتور آزمون تعاملی: چندگزینه‌ای با بازخورد فوری سبز/قرمز، توضیح آموزشی هر پرسش، نوار پیشرفت، زمان‌سنج، حد نصاب قبولی، ستاره، بهترین رکورد، شروع دوباره، مرور پاسخ‌های نادرست، نشان‌کردن پرسش‌ها (🚩) |
| ۳ | **هیچ بازخورد انگیزشی نیست** — کاربر نمره و پیشرفت عددی نمی‌بیند | داشبورد یادگیری در صفحه اصلی: درصد پیشرفت دوره، تعداد فصل‌های تمام‌شده، آزمون‌های داده‌شده، میانگین نمرات، بهترین رکورد هر فصل + نشان نمره روی کارت هر فصل |
| ۴ | **جایی برای یادداشت شخصی نیست** | بخش ۱۶ «یادداشت‌های من» در هر فصل با ذخیره خودکار در localStorage |

### ب) تجربه کاربری و فنی

| # | کمبود | راه‌حل در قالب جدید |
| --- | --- | --- |
| ۵ | نبود حالت تاریک (مطالعه شبانه) | تم روشن/تاریک با متغیرهای CSS، کلید میان‌بر `d`، ذخیره انتخاب، احترام به تنظیم سیستم |
| ۶ | اندازه فونت ثابت | سه سطح اندازه متن با دکمه‌های ＋/－ |
| ۷ | کدها بدون رنگ‌آمیزی نحو | هایلایتر سبک داخلی برای پایتون و بش (بدون کتابخانه خارجی) |
| ۸ | نبود میان‌بر کیبورد مستند | مودال راهنمای میان‌برها (`?`) + `/` برای جست‌وجو |
| ۹ | نبود سیستم پیام‌رسانی | توست (Toast) برای همه رویدادها: کپی شد، ذخیره شد، نمره آزمون و... |
| ۱۰ | دسترسی‌پذیری ضعیف | لینک پرش (Skip Link)، `aria-label`، فوکوس‌پذیری واژه‌ها با Tab، پشتیبانی `prefers-reduced-motion` |
| ۱۱ | هدر ثابت نبود | هدر چسبان (Sticky) در همه صفحات |
| ۱۲ | پراکندگی منطق در یک `script.js` | تفکیک به ماژول‌ها: `util.js` / `glossary-data.js` / `glossary.js` / `quiz.js` / `app.js` |

### ج) نکات محتوایی/نگارشی مشاهده‌شده در فایل‌های موجود

- عنوان فصل ۹: «as_view و personalization با get_context_data» → واژه انگلیسی وسط فارسی؛ درست: «شخصی‌سازی با get_context_data»
- در README: «بازگشت به عقب و مدیریت تغییرات بدون از دست دادن داده» → روان‌تر: «بدون از دست رفتن داده»
- در متن فصل‌ها (طبق README) «Migrationها» → یکدست‌سازی به «مهاجرت‌ها (Migrations)» پیشنهاد می‌شود
- نیم‌فاصله‌ها در اکثر متن‌ها رعایت شده؛ در قالب جدید هم یکدست حفظ شده است

---

## 📐 جمع‌بندی

اسکلت فعلی **پایهٔ فنی سالمی** دارد (آفلاین، واکنش‌گرا، ذخیره پیشرفت) و مشکل اصلی آن **کمبود لایه‌های یادگیری فعال** است: واژه‌نامه، آزمون تعاملی، بازخورد و انگیزش. قالب نسخه ۲ دقیقاً همین لایه‌ها را اضافه می‌کند، بدون شکستن ساختار قبلی — کلید `djc_finished_v1` سازگار نگه داشته شده تا پیشرفت کاربران قبلی حفظ شود.

مستندات کامل قالب جدید: [`TEMPLATE-GUIDE.md`](./TEMPLATE-GUIDE.md)
